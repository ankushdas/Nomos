(executable
 (name nomos)
 (libraries core clp)
 (preprocess (pps ppx_let)))

(menhir
  (modules Parser)
  (flags --explain)
  (infer true))

(ocamllex lexer)

(env
    (dev
      (flags (:standard -warn-error -A))))

(executables
 (names rast regression)
 (libraries core clp)
 (preprocess (pps ppx_let)))

 (install
 (section bin)
 (package rast)
 (files (main.exe as rast)))

(env
    (dev
      (flags (:standard -warn-error -A))))
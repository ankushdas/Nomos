(library
 (name lib)
 (libraries core clp)
 (preprocess (pps ppx_let ppx_sexp_conv)))

(menhir
  (modules Parser)
  (flags --explain)
  (infer true))

(ocamllex Lexer)

(env
    (dev
      (flags (:standard -warn-error -A -w -26-27))))
